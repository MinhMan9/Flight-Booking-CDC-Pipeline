# Nothing
This markdown file stand here to avoid Spark container can't found `/jars` folder when you not yet downloaded the necessary `.jar` files but run the `docker-compose up` command
And remmember to run `./download_jars.sh` before running any spark script in `/spark/scripts`